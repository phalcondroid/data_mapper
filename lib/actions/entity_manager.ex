defmodule DataMapper.Actions.EntityManager do
    alias DataMapper.Actions.Assembler
    def find(params \\ %{}) do
        
    end

    def find_one(params \\ %{}) do
        
    end

    def count(params \\ %{}) do
        
    end

    def save(struct) do
        
    end
end